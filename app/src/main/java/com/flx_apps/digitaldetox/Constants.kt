package com.flx_apps.digitaldetox

/**
 * The number of milliseconds in one minute, used to reload the active features every minute.
 * (Calculating this value every time would be a waste of resources.)
 */
const val OneMinuteInMs = 60 * 1000L